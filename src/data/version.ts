// Auto-updated by Claude — 2026-08-13 (v19.12)
export const VERSION = '19.12.0';
export const LAST_UPDATED = '2026-08-13';
export const LAST_PUSH = 'v19.12: headless vault-only sync (Aug 13). Two insights from one session: survey distribution design review (ins-ce-aug13-01) — three evaluatee-display variants queued as screenshots for offline review, a missing multiple-instructor add/remove example flagged, and email template redesign explicitly deferred to the Settings phase over unresolved merge-field placement; course-eval design-system upgrade process (ins-ce-aug13-02) — manual component updates plus a new content-preservation agent replace the rolled-back auto-update hook, Vinay\'s new component set to be shared by Exam Management and Course Evaluation, Aug 17 named as the critical deadline to unblock engineering on survey distribution. Anchor was Aug 11; window covered Aug 12-13. Vault mtimes are bulk-touched on nearly every file (known from v19.10), so staleness was verified by filename/frontmatter date, not raw mtime — all four Aug 12 meeting notes found in-window were already synthesized in v19.11 (completion dashboard, five-role access model, drawer-pattern survey design, PCE Steps 2-4 spec), and the Aug 12 Design/PM-collaboration session remains excluded per the same privacy flag raised in v19.11. Headless run: Granola MCP unavailable, Obsidian vault only; tsc + build served as the gate (no Chrome visual spot-check). check-themes could not be run interactively in this session (approval unavailable); tsc --noEmit and npm run build both passed clean, and themeId/tags were hand-verified against src/data/themes.ts and src/types/index.ts before publishing. v19.11: Granola + vault sync (Aug 12). Five new insights from four sessions across Aug 10-11. Design system utility bar and home redesign (Aug 10, ins-ds-aug10-01): hybrid convergence approach confirmed as the shared navigation framework across old-Prism and new-design-system modules — new modules adopt the new DS fully, old Prism modules keep their look, the top bar is the unifying handle; top-bar gaps flagged (Ask Leo prominence, chat icon, per-product Onboarding Notes, program/tenant selector); home redesign scope corrected to only confirmed Jan 1, 2027 deliverables after speculative 12+ month-out features were flagged as a risk. Course Eval sync up (Aug 11, ins-ce-aug11-04): PCE Steps 2-4 spec locked for dev handoff — Step 2 toggle at role level, single-template-per-course model; Step 3 gains survey title/instructions and a per-course window override, loses reminder frequency and result release date; Step 4 redesigned as a course-offering list view; Manage Survey flagged as the next blocked priority. Two further Aug 11-12 sessions: completion-vs-results dashboard split with a shared five-stage status vocabulary and two configurable response-rate thresholds (ins-ce-aug11-01); five-role PCE access model with per-role login experience and status terminology (ins-ce-aug11-02). Survey design screen (Aug 11, ins-ce-aug11-03): drawer pattern replacing inline accordion, aspects as groupings not atomic rows, individual-faculty-within-an-aspect left unresolved, and the duplicate-aspect hard block reconfirmed — added as a second evidence link on the still-open conf-duplicate-mode conflict against the Jul 24 soft-warning claim. One low-value session (Aug 11 exam management solution review, a deferred walkthrough with no substantive content) and one astrology/personal session were excluded per the sync skill\'s scope rules; a Design/PM-collaboration session touching Romit\'s individual performance was flagged to the user rather than published, pending an explicit decision on whether/how sensitive performance content belongs on a public site. check-themes passed: 439 insights, all themed, tier counts P0:78 P1:134 P2:170 P3:57. v19.10: headless vault-only sync (Aug 10). One insight closes a previously untracked open dispute: the Jul 22 PCE template-builder layout disagreement between Monil (horizontal aspect-tabs) and Romit (nested templates, objecting to tab chrome above content) is resolved by source-code verification — Variant B "guided rail" is confirmed shipped as the literal default (components/pce/template-editor.tsx:418), keeping Monil\'s Course→Faculty→General stop order inside a left checklist rail (ins-ce-aug07-01). Sourced from the vault Decisions/pce/ note (resolution dated Aug 7), not a new raw session — stakeholder sign-off itself was not independently re-verified. Headless run: Granola MCP unavailable, Obsidian vault only; tsc + build served as the gate (no Chrome visual spot-check). v19.9: PCE Step 2 design sync (Aug 4). Four insights from the Step two design session: template-switch on the setup dashboard confirmed as override-vs-new-survey (ins-ce-aug04-01); aspect deduplication specified as a hard block — system auto-deselects a duplicate course-content aspect and routes the admin back to the existing template rather than allowing a competing one (ins-ce-aug04-02), which reads in direct tension with the Jul 24 decision to use a soft, overridable warning for duplicate evaluatees (ins-ce-jul24-03) — opened as conf-duplicate-mode in conflicts.ts rather than silently picked; auto-update toggle for post-setup Prism faculty inclusion plus mid-setup draft-resume behavior confirmed (ins-ce-aug04-03); unassigned-faculty visibility gap flagged with a proposed external list-view, not yet approved (ins-ce-aug04-04). All four themeId:form-experience/navigation-ia, evidence spans captured verbatim from the design note. check-themes passed: 433 insights, all themed, tier counts P0:78 P1:129 P2:169 P3:57. v19.8: Vault reconcile + Jul 30-31 sync. The research-repos Obsidian vault was stuck 19 days behind origin — uncommitted local edits to _Insights.md and MOC-decisions.md were correctly blocking exxat-vault-pull\'s safe fast-forward on every scheduled run since Jul 13, even though the cloud granola-obsidian-sync routine kept committing fine upstream. Reconciled locally: stashed the local curation (pruned 4 recurring duplicate meeting entries), fast-forwarded 11 incoming commits, reapplied the prune on top, recomputed section counts (266 notes / 172 high-value), added 6 untracked local decision notes, pushed. 9 new insights synthesized from the two sessions that landed since the prior anchor: Exam management weekly call (Jul 30) — repurpose-first assessment creation confirmed as the primary path (scratch is the rare case), AI blueprint intake keyed off course objectives/Bloom\'s level with institution-variable terminology, question bank auto-filtered by lecture range but never hard-restricted, pre-assessment point-biserial flagging validated as an admin-configurable quality gate ahead of the existing post-exam item analysis; Exact Prism evaluation setup (Jul 31) — setup dashboard confirmed as the master configuration screen with three explicit states (not-configured/in-progress/distributed), settling the dense-table-vs-simplified-wizard debate in favor of density; in-workflow faculty edit removes a Prism round-trip; bulk-select restricted to unconfigured courses only; template-shortcut gap flagged for cold-start dashboards. v19.7: Tier-2 closeout. Conflict queue: conflicts.ts generalizes the COHERE pattern — 3 open conflicts (Cohere gate date, communication-rules location, offline-download scope) each with both claims, owner, what it blocks, and evidence links; rendered on /decisions, listed in the digest, served by the MCP open_conflicts tool; check-themes validates evidence refs; sync-sources records new conflicts there and resolves without deleting. Corpus hygiene: the 7 duplicate insight ids renamed (second occurrences get -b suffix; the one recommendation reference verified to mean the first) and check-themes now FAILS on any duplicate id. Term watch (Channels-lite): 8 curated watch terms with monthly mention sparklines + 30d momentum in Analytics — templates +10 this month. Evidence debt by theme: direct-quote share ranked weakest-first, the floor the evidence-span backfill must raise; sync-sources now backfills spans for existing insights whenever a session transcript is in context. v19.6: Stakeholder Atlas (/stakeholders) — the missing internal-persona layer. The 4 personas are research subjects; this is the 14 decision-makers and domain voices whose words drive the corpus (Aarti, Arun, Kunal, Vishaka, Nipun, Vishal, Monil, Akshit, David, Himanshu, Darshan, Harsha, Prasanjit, Pratiksha). Curated identity: role, group (leadership/product/design-eng/domain-SME), authority (every claim backed by insight-id evidence links), and what their voice means for the product. Computed live by name-match over text/source/quotes: influence mass tier-stacked (Aarti 999 · Vishaka 682 · Nipun 620), stakeholder × theme footprint heat grid (footprint vs title comparison), decision-tagged mention counts, recommendations owned (links to /decisions), top-theme chips per person. 56% of the corpus involves at least one stakeholder. Every number opens the search that produces it (q= name + theme filters). v19.5: advanced viz (competitor-gap closure). Theme Trend Explorer: per-theme aligned sparklines on the shared month domain + 30d-vs-prior deltas (Dovetail-Channels-style longitudinal theming; ranked-sparkline rows, not 12-line spaghetti). Priority quadrant: the backlog-aging scatter gains reference lines at 90d and the P0 floor (24) — top-right is the named danger quadrant. Evidence valence: pain (gap-tagged) vs opportunity share by month, tag-derived and labeled as such; optional Insight.sentiment field + sync rule added for the real thing. Evidence flow: hand-rolled 3-column sankey product → theme → P0–P3 tier (neutral ribbons, entity-colored nodes, true-count labels). Journey friction map: persona × 7 curated workflow stages (journeys.ts, signal-style curated defs + computed membership) as a linked heat grid on the Persona Atlas — DCE breaks at Build/Navigate, SCCE row exposes the coverage gap. Chart contracts from ins-ds-jul-01 finally honored: every Fig ships PNG export + CSV raw-data export + optional expandable detail. Charts view rebuilt as a dimension dashboard: opportunity-mass tier-stacked bars (ranked by summed score, not row count — TierBars) + severity mix + volume small multiples shown together; theme dimension added. Knowledge Graph rebuilt from scratch: the hand-curated 44-node force hairball is retired; the new graph is computed theme co-occurrence (edge = two themes surfacing in the same session, ≥2 sessions), circular deterministic layout, labeled nodes sized by evidence mass, focus-a-theme chips, and a strongest-connections list where every pair opens its shared-session insights. v19.4: the agentic-repository upgrade (from the research-repo competitive analysis, docs/research/). Decisions layer: recommendations are first-class objects (src/data/recommendations.ts) — 10 seeded from the corpus, each with an evidence chain of real insight ids (validated by check-themes), owner, and a status pipeline mirroring the actual approval process (proposed → aligned → approved → shipped/rejected); new /decisions view groups the pipeline with every evidence count opening its insights. Weekly digest (/digest): computed live — last-7d insights by theme with links, new P0s, products going quiet (14d+ stale), open conflicts (Cohere date), recommendations stalling 30+ days. Evidence spans: Insight.evidence[] carries verbatim transcript excerpts (highlight-level provenance); doc page renders an evidence drawer; sync-sources captures 1–3 spans per new insight and spans upgrade evidence class to DIRECT QUOTE. MCP server (scripts/mcp-server.ts + .mcp.json): the corpus as 7 tools (search_insights with theme/tier/signal filters, get_insight, corpus_overview, list_themes/signals/recommendations, open_conflicts) over the same selectors the site renders from — ask-the-repository from Claude Code/Cursor with insight-id citations. v19.3: theme taxonomy + score tiers. 12 first-class themes (src/data/themes.ts) with a REQUIRED themeId on every insight — all 420 classified in a one-time pass, coverage enforced by the compiler and scripts/check-themes.ts (each theme 8–78 members, none >40% of corpus); the 7 signals stay as the strategic layer (previously they covered only 124/420). Insight Index: theme chips join the filter row (URL-synced), new flat ↔ by-theme view toggle — grouped mode sections the current query by theme, ordered by opportunity mass, with per-product counts + persona segment chips and top-5 rows. Product hubs: "Themes in this product" strip (live counts, every chip links to the query). Scoring made honest: the P7 score (severity×evidence×persona, 11 possible values, 65% of corpus on 12/18) now renders as P0–P3 tiers (P0 ≥24 · P1 ≥18 · P2 ≥12 · P3 <12; counts 75/124/164/57) with the inspectable formula (e.g. 4×2×3 = 24) beside the token — bare numbers eliminated from index + insight doc; numeric total still drives sort order. sync-sources SKILL updated to assign themeId on every future insight. v19.0: visualization-first redesign of all 24+ views. Latest-findings system: every product hub opens with a "What\'s new" feed (newest evidence, momentum sparkline, 30d trend delta, staleness meter) and the Command Center leads with a cross-product freshness strip. Data integrity: declared insightCount/criticalGaps removed from ProductMeta — every count derives live via productFacts/corpusFacts (briefings and hubs now agree: 245/43); 348-subset get* helpers deleted; milestone state derives from dates. Link contract: since/until date params + monthRange + parseInsightFilter — time-series marks open their month\'s rows as removable date tokens; QueryLink component makes context-free "→" links unrepresentable. Chart kit v2: Sparkline, TrendDelta, SmallMultiples, DivergingBarChart (true −100..+100 NPS bars replace the +100-shift hack), interactive tooltips/legends everywhere, n= badges + sparse-data notes on every Fig. Story kit: EvidenceRow/FindingsFeed (Item-based rows kill the card soup), StatTiles, Citation QuoteRows (Highlights raw-div masonry deleted), ConflictNote (Cohere date rendered as the contested fact it is), StalenessMeter (Skills/LC honestly read dormant). Spec architecture: SpecPageHeader (claim lede + live corpus chips + one orienting visual) on all 12 spec surfaces, SectionTabs everywhere, prose card walls → tables/heat grids (NavIA access matrix, Skills method coverage, LC per-persona evidence that tests — and corrects — the SCCE claim). Roadmap rebuilt as a real date-axis milestone chart (product lanes, today line, hard deadlines ringed; LC honestly shows 0 dated milestones). Platform orphan route fixed. tsc 0 errors; production build green.';
export const PUSH_METHOD = 'Direct git push via PAT — zero manual steps';
export const SKILL_VERSION = '5.0.0';
export const SESSIONS_SYNCED = 84;
export const INSIGHTS_TOTAL = 441; // = ALL_INSIGHTS.length; keep in step when adding insights
export const PRODUCTS_COVERED = 5;
export const MAGIC_PATTERNS_PCE = 'https://project-truthful-apricot-326.magicpatterns.app';
export const MAGIC_PATTERNS_EXAM_ADMIN = 'https://project-rich-macaroni-332.magicpatterns.app';
export const MAGIC_PATTERNS_UNIVERSITY = 'https://project-precious-cranberry-828.magicpatterns.app';
export const EXXATLY_DS_URL = 'https://www.magicpatterns.com/design-system/ds-712de3ba-b73d-407d-9d94-a149d8f9d481';
export const VIEWS_COUNT = 27;
export const SESSIONS_RAW_READ = 76;
export const DS_TOKENS_CONFIRMED = 263;
export const DS_COMPONENTS_BUILT = 31;
export const DS_COMPONENTS_PHASE2 = 11;
